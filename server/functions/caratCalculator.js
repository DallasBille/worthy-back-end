class caratCalculator {
  caratCalculation() {
    let value = 7291; // Here we assume 1100 is the return of clarity calculation.
    let firstIncrease = value * fakePostRequest.carat;
    let finalSoldForPrice = firstIncrease * fakePostRequest.carat;
    return Math.round(finalSoldForPrice);
  }
}

export default caratCalculator;
